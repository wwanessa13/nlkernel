% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_g_pc_lpc.R
\name{env_g_pca_laplacian}
\alias{env_g_pca_laplacian}
\title{Kernel PCA with Laplacian Kernel for Multi-Environment Genomic Prediction}
\usage{
env_g_pca_laplacian(
  SNPs,
  y,
  IDs,
  env,
  EZ = NULL,
  CV = c("CV1", "CV2", "CV0"),
  sg = c(0.001, 0.01, 0.05, 0.1),
  var_threshold = 0.01,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  save_xlsx = TRUE,
  file_name = NULL
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes, with genotypes in rows and markers in columns.
Row names must correspond to genotype IDs.}

\item{y}{A numeric vector of phenotypic values.}

\item{IDs}{A vector of genotype IDs corresponding to each phenotypic observation.}

\item{env}{A vector of environment labels corresponding to each phenotypic observation.}

\item{EZ}{Optional matrix of fixed environmental effects. If NULL, it is created from env.}

\item{CV}{Cross-validation scheme. One of "CV1", "CV2", or "CV0".}

\item{sg}{A numeric vector of sigma values for the Laplacian kernel. Default is c(0.001, 0.01, 0.05, 0.1).}

\item{var_threshold}{Minimum proportion of variance explained required for a principal component to be retained. Default is 0.01.}

\item{nIter}{Total number of iterations for the BGLR model. Default is 10000.}

\item{burnIn}{Number of burn-in iterations for the BGLR model. Default is 4000.}

\item{thin}{Thinning interval for the BGLR model. Default is 10.}

\item{save_xlsx}{A logical value indicating whether to save results in an Excel file. Default is TRUE.}

\item{file_name}{Character string specifying the name of the Excel file. If NULL, a default name is used.}
}
\value{
A data frame with the mean predictive capacity by model, CV scheme,
sigma value, number of selected PCs, and environment.
}
\description{
This function fits kernel PCA models using a Laplacian kernel for
multi-environment genomic prediction. It evaluates different sigma values.
Kernel principal components are selected according to a variance-explained
threshold, and a genomic kernel is constructed from the selected component
scores. The kernel is expanded to the observation level and combined with
fixed environmental effects. Predictive capacity is evaluated by environment
using CV1, CV2, or CV0 cross-validation schemes within the RKHS framework
implemented in BGLR.
}
