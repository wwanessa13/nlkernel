% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_ge_pc_gauss.R
\name{env_ge_pca_gaussian}
\alias{env_ge_pca_gaussian}
\title{Kernel PCA with Gaussian Kernel for Multi-Environment Genomic Prediction with GxE Interaction}
\usage{
env_ge_pca_gaussian(
  SNPs,
  y,
  IDs,
  env,
  EZ = NULL,
  CV = c("CV1", "CV2", "CV0"),
  sg = c(0.001, 0.01, 0.05, 0.1),
  var_threshold = 0.01,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  save_xlsx = TRUE,
  file_name = NULL
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes, with genotypes in rows and markers in columns.
Row names must correspond to genotype IDs.}

\item{y}{A numeric vector of phenotypic values.}

\item{IDs}{A vector of genotype IDs corresponding to each phenotypic observation.}

\item{env}{A vector of environment labels corresponding to each phenotypic observation.}

\item{EZ}{Optional matrix of fixed environmental effects. If \code{NULL}, it is created from \code{env}.}

\item{CV}{A character string specifying the cross-validation scheme:
"CV1": Prediction of unobserved genotypes in observed environments.
"CV2": Prediction of genotypes observed in only a subset of environments.
"CV0": Prediction of observed genotypes in completely unobserved environments.}

\item{sg}{A numeric vector of sigma values for the Gaussian kernel.
Default is \code{c(0.001, 0.01, 0.05, 0.1)}.}

\item{var_threshold}{Minimum proportion of variance explained required for a principal component to be retained. Default is 0.01.}

\item{nIter}{Total number of iterations for the BGLR model. Default is 10000.}

\item{burnIn}{Number of burn-in iterations for the BGLR model. Default is 4000.}

\item{thin}{Thinning interval for the BGLR model. Default is 10.}

\item{save_xlsx}{A logical value indicating whether to save results in an Excel file. Default is \code{TRUE}.}

\item{file_name}{Character string specifying the name of the Excel file. If \code{NULL}, a default name is used.}
}
\value{
A data frame with the mean predictive capacity by model, CV scheme,
sigma value, number of selected PCs, and environment, accounting for the
GxE interaction model.
}
\description{
This function fits kernel PCA models using a Gaussian kernel for
multi-environment genomic prediction. It evaluates different sigma values.
Kernel principal components are selected according to a variance-explained
threshold, and a genomic kernel is constructed from the selected component
scores. The kernel is expanded to the observation level and combined with
fixed environmental effects and a Genotype by Environment (GxE) interaction
kernel.
}
\details{
The model implemented is:
\deqn{y = Xb + Zg + Zi + e}
where \eqn{Xb} represents fixed environmental effects, \eqn{Zg} represents the
main genomic effect obtained from the KPCA Gaussian kernel, and \eqn{Zi}
represents the GxE interaction effect. The GxE kernel is computed as the
Hadamard product between the observation-level KPCA Gaussian genomic kernel
and the environmental relationship matrix.
}
\examples{
\dontrun{
results <- env_ge_pca_gaussian(
  SNPs = X,
  y = phen$yield,
  IDs = phen$genotype,
  env = phen$Env,
  CV = "CV2"
)
}

}
