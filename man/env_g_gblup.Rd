% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_g_gblup.R
\name{env_g_gblup}
\alias{env_g_gblup}
\title{Multi-Environment Genomic Prediction via GBLUP}
\usage{
env_g_gblup(
  SNPs,
  y,
  IDs,
  env,
  EZ = NULL,
  CV = c("CV1", "CV2", "CV0"),
  ploidy = 2,
  maf = 0.05,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  save_xlsx = TRUE,
  file_name = NULL
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes (individuals in rows, markers in columns).
Must have \code{rownames} corresponding to the genotype IDs.}

\item{y}{A numeric vector of phenotypic values.}

\item{IDs}{A character vector indicating the genotype identity for each observation in \code{y}.}

\item{env}{A character vector indicating the environment for each observation in \code{y}.}

\item{EZ}{An incidence matrix for fixed environmental effects. If \code{NULL},
it is automatically generated from the \code{env} vector.}

\item{CV}{A character string specifying the cross-validation scheme:
\itemize{
\item \code{"CV1"}: Prediction of unobserved genotypes in observed environments.
\item \code{"CV2"}: Prediction of genotypes observed in only a subset of environments.
\item \code{"CV0"}: Prediction of observed genotypes in completely unobserved environments.
}}

\item{ploidy}{Integer. The ploidy level of the species. Default is 2 (diploid).}

\item{maf}{Numeric. Minor Allele Frequency threshold to filter SNPs. Default is 0.05.}

\item{nIter}{Total number of iterations for the BGLR Gibbs sampler. Default is 10000.}

\item{burnIn}{Number of burn-in iterations to be discarded. Default is 4000.}

\item{thin}{Thinning interval for the MCMC chain. Default is 10.}

\item{save_xlsx}{Logical. If \code{TRUE}, saves the predictive capacity results to an Excel file. Default is \code{TRUE}.}

\item{file_name}{Character string for the Excel file name. If \code{NULL}, a name
is automatically generated as "gblup_\link{CV_scheme}.xlsx". Default is \code{NULL}.}
}
\value{
A dataframe containing the predictive capacity (mean Pearson correlation)
for each environment under the specified cross-validation scheme.
}
\description{
This function performs genomic prediction across multiple environments using the
Genomic Best Linear Unbiased Prediction (GBLUP) method. It constructs a genomic
relationship matrix based on the VanRaden (2008) method and implements
a main effects model through the BGLR Gibbs sampler.
}
\details{
The function uses the \code{AGHmatrix} package to compute the VanRaden GRM and
\code{BGLR} for the Bayesian MCMC estimation. The model includes fixed environmental
effects and random genomic effects.
}
\examples{
\dontrun{
# Example usage:
results <- env_g_gblup(
  SNPs = X,
  y = data$yield,
  IDs = data$id,
  env = data$location,
  CV = "CV1"
)
}

}
