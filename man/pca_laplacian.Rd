% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_lpc.R
\name{pca_laplacian}
\alias{pca_laplacian}
\title{Kernel PCA with Laplacian Kernel for Genomic Prediction}
\usage{
pca_laplacian(
  SNPs,
  y,
  sigmas = c(0.001, 0.01, 0.05, 0.1),
  n_folds = 5,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  seed = 123,
  save_xlsx = TRUE,
  file_name = "pca_laplacian.xlsx"
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes, with individuals in rows and markers in columns.}

\item{y}{A numeric vector of phenotypic values corresponding to the individuals.}

\item{sigmas}{A numeric vector of sigma values for the Laplacian kernel. Default is c(0.001, 0.01, 0.05, 0.1).}

\item{n_folds}{Number of folds for cross-validation. Default is 5.}

\item{nIter}{Total number of iterations for the BGLR model. Default is 10000.}

\item{burnIn}{Number of burn-in iterations for the BGLR model. Default is 4000.}

\item{thin}{Thinning interval for the BGLR model. Default is 10.}

\item{seed}{Integer value used to set the random seed for reproducibility.
Different seed values generate different random partitions of the dataset
into cross-validation folds. Default is 123.}

\item{save_xlsx}{A logical value indicating whether to save results in an Excel file. Default is TRUE.}

\item{file_name}{Character string specifying the name of the Excel file. Default is "pca_laplacian.xlsx".}
}
\description{
This function fits kernel PCA models using a Laplacian kernel for genomic
prediction. It evaluates different sigma values for the Laplacian kernel.
All kernel principal components returned by the KPCA decomposition are used
to construct a kernel matrix from the component scores. Predictive accuracy
is evaluated using k-fold cross-validation with the RKHS framework
implemented in the BGLR package.
}
