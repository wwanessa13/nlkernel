% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_ge_gblup.R
\name{env_ge_gblup}
\alias{env_ge_gblup}
\title{Multi-Environment Genomic Prediction via GBLUP with GxE Interaction}
\usage{
env_ge_gblup(
  SNPs,
  y,
  IDs,
  env,
  EZ = NULL,
  CV = c("CV1", "CV2", "CV0"),
  ploidy = 2,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  save_xlsx = TRUE,
  file_name = NULL
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes (individuals in rows, markers in columns).
Must have \code{rownames} corresponding to the genotype IDs.}

\item{y}{A numeric vector of phenotypic values.}

\item{IDs}{A character vector indicating the genotype identity for each observation in \code{y}.}

\item{env}{A character vector indicating the environment for each observation in \code{y}.}

\item{EZ}{An incidence matrix for fixed environmental effects. If \code{NULL},
it is automatically generated from the \code{env} vector.}

\item{CV}{A character string specifying the cross-validation scheme:
"CV1": Prediction of unobserved genotypes in observed environments.
"CV2": Prediction of genotypes observed in only a subset of environments.
"CV0": Prediction of observed genotypes in completely unobserved environments.}

\item{ploidy}{Integer. The ploidy level of the species. Default is 2.}

\item{nIter}{Total number of iterations for the BGLR Gibbs sampler. Default is 10000.}

\item{burnIn}{Number of burn-in iterations to be discarded. Default is 4000.}

\item{thin}{Thinning interval for the MCMC chain. Default is 10.}

\item{save_xlsx}{Logical. If \code{TRUE}, saves the predictive capacity results to an Excel file. Default is \code{TRUE}.}

\item{file_name}{Character string for the Excel file name. If \code{NULL}, a name
is automatically generated as "gblup_CV(1, 2 or 0).xlsx". Default is \code{NULL}.}

\item{seed}{Integer value used to set the random seed for reproducibility in
CV1 and CV2. Different seed values generate different random partitions of
the dataset into cross-validation folds. Default is 123.}
}
\description{
This function performs genomic prediction across multiple environments by
accounting for main effects (Genotype and Environment) and the Genotype by
Environment (GxE) interaction. It uses a VanRaden-based genomic relationship
matrix and an environmental kernel to model the interaction via a Hadamard product.
}
\details{
The model implemented is:
\deqn{y = Xb + Zg + Zi + e}
where \eqn{Xb} represents fixed environmental effects, \eqn{Zg} represents the
main genomic effect modeled with the G matrix (Van Raden, 2008), and \eqn{Zi} represents
the GxE interaction effect. The GxE interaction is computed as the Hadamard product
between the observation-level genomic matrix (G) and the environmental
relationship matrix (E).
}
\examples{
\dontrun{
# Example usage:
results <- env_ge_gblup(
  SNPs = X,
  y = phen$yield,
  IDs = phen$genotype,
  env = phen$Env,
  CV = "CV2"
)
}

}
