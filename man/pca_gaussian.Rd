% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pc_gauss.R
\name{pca_gaussian}
\alias{pca_gaussian}
\title{Kernel PCA with Gaussian Kernel for Genomic Prediction}
\usage{
pca_gaussian(
  SNPs,
  y,
  sigmas = c(0.001, 0.01, 0.05, 0.1),
  var_threshold = 0.01,
  n_folds = 5,
  nIter = 10000,
  burnIn = 4000,
  thin = 10,
  save_xlsx = TRUE,
  file_name = "pca_gaussian.xlsx"
)
}
\arguments{
\item{SNPs}{A numeric matrix of SNP genotypes, with individuals in rows and markers in columns.}

\item{y}{A numeric vector of phenotypic values corresponding to the individuals.}

\item{sigmas}{A numeric vector of sigma values for the Gaussian kernel. Default is c(0.001, 0.01, 0.05, 0.1).}

\item{var_threshold}{Minimum proportion of variance explained required for a principal component to be retained. Default is 0.01.}

\item{n_folds}{Number of folds for cross-validation. Default is 5.}

\item{nIter}{Total number of iterations for the BGLR model. Default is 10000.}

\item{burnIn}{Number of burn-in iterations for the BGLR model. Default is 4000.}

\item{thin}{Thinning interval for the BGLR model. Default is 10.}

\item{save_xlsx}{A logical value indicating whether to save results in an Excel file. Default is TRUE.}

\item{file_name}{Character string specifying the name of the Excel file. Default is "pca_laplacian.xlsx".}
}
\value{
A list with:
\describe{
\item{results}{A data frame with the mean and standard deviation of predictive accuracy for each sigma value.}
\item{predictions}{A list of data frames with observed and predicted values for each fold and sigma value.}
\item{folds}{A numeric vector indicating the fold assignment for each individual.}
}
}
\description{
This function fits kernel PCA models using a Gaussian kernel for genomic
prediction. It evaluates different sigma values for the Gaussian kernel.
Principal components are selected according to a variance-explained threshold,
and a kernel matrix is constructed from the selected component scores.
Predictive accuracy is evaluated using k-fold cross-validation with the RKHS
framework implemented in the BGLR package.
}
